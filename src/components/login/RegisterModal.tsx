import React from "react";

export const RegisterModal: React.FC<RegisterProps> = (props) => {
	return (
		<div className="w-[500px] bg-white border shadow-md filter opacity-100 visible">
			RegisterModal
		</div>
	);
};

type RegisterProps = {};
