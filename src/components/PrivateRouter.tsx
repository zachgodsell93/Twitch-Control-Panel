import React from "react";
import PropTypes from "prop-types";

const PrivateRouter = (props: PrivateRouterPropTypes) => {
	return <div>PrivateRouter</div>;
};

type PrivateRouterPropTypes = {};

export default PrivateRouter;
